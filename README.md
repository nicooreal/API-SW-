## Variables de entorno

Este proyecto utiliza variables de entorno para configurar la API.

Crear un archivo `.env` en la raíz del proyecto con el siguiente contenido:

VITE_SWAPI_BASE_URL=https://www.swapi.tech/api
